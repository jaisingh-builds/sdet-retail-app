# Planned Services

The current runnable starter API lives in `backend/`.

The planned implementation will move toward:

- `services/pos-service`: NestJS POS/retail API.
- `services/oms-service`: NestJS order management service.

This structure supports the Week 4 and Week 7 planner requirements for microservices, Pact contract testing, CI contract gates, and resilience validation.
