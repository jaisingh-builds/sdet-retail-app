services:
  retail-api:
    build:
      context: ./backend
    environment:
      PORT: 4000
    ports:
      - "4000:4000"

  wiremock:
    image: wiremock/wiremock:3.6.0
    ports:
      - "8089:8080"
    volumes:
      - ./wiremock:/home/wiremock
